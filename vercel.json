{
    "functions": {
        "api/webhook.ts": {
            "includeFiles": "src/**",
            "maxDuration": 60
        }
    },
    "routes": [
        {
            "src": "/api/webhook",
            "dest": "api/webhook.ts"
        }
    ]
}
