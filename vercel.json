{
    "functions": {
        "api/webhook.ts": {
            "includeFiles": "src/**",
            "maxDuration": 300
        }
    },
    "routes": [
        {
            "src": "/api/webhook",
            "dest": "api/webhook.ts"
        }
    ]
}
